smb2passchk

A tool, to check a username password combination against a
WindowNT Domain.

First build samba with the following configure line f.e.:

configure --prefix=/opt/samba --with-libsmbclient -without-ads -without-syslog -without-smbmount -without-krb5

Next you can build in examples/smb3passchk the tool with
a simple 'make'
